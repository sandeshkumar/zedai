// The site-wide footer is the coastal redesign's footer (Kudla skyline and
// sunset wordmark). Kept under the old name so every page picks it up.
export { SiteFooter as Footer } from "@/components/home/SiteFooter";
