// The site-wide navigation is the coastal redesign's nav. Kept under the old
// name so every page picks it up without changing its imports.
export { SiteNav as Navbar } from "@/components/home/SiteNav";
